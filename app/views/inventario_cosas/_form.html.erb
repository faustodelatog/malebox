<%= form_for(@inventario_cosa) do |f| %>
  <% if @inventario_cosa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inventario_cosa.errors.count, "error") %> prohibited this inventario_cosa from being saved:</h2>

      <ul>
      <% @inventario_cosa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cosa_id %><br>
    <%= f.collection_select(:cosa_id, Cosa.all, :id, :nombre, {},  class: "form-control") %>
  </div>
  <div class="field">
    <%= f.label :inventario %><br>
    <%= f.select(:inventario, InventarioCosa.inventarios.keys.map { |w| [w.humanize, w]}, {}, class: "form-control") %>
  </div>
  <div class="field">
    <%= f.label :cantidad %><br>
    <%= f.number_field :cantidad, class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
