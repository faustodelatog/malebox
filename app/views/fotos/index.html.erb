<p id="notice"><%= notice %></p>

<h1>Listing Fotos</h1>

<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Url</th>
      <th>Producto</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @fotos.each do |foto| %>
      <tr>
        <td><%= foto.producto.nombre %></td>
        <td><img src="<%= foto.url %>" width="100" /></td>
        <td><%= link_to 'Show', foto %></td>
        <td><%= link_to 'Edit', edit_foto_path(foto) %></td>
        <td><%= link_to 'Destroy', foto, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Foto', new_foto_path %>
