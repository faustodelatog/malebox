<p id="notice"><%= notice %></p>

<h1>Listado de Pedidos</h1>

<div id="filtros">
  <form action="<%= pedidos_path %>">
      <div class="col-md-4">
        <label class="col-form-label-sm">Estado</label>
        <select class="form-control" name="estado">
            <option value="">TODOS</option>
          <% @estados.each do |estado| %>
            <option value="<%= estado.upcase %>"><%= estado.upcase %></option>
          <% end %>
          </select>
      </div>
      <div class="col-md-4">
        <label class="col-form-label-sm">Desde</label>
        <input class="form-control" type="text" name="from" value="<%= @from %>" />
      </div>
      <div class="col-md-4">
        <label class="col-form-label-sm">Hasta</label>
        <input class="form-control" type="text" name="to" value="<%= @to %>" />
      </div>
      <div class="col-md-12">
          <label class="col-form-label-sm"></label>
        <button type="submit" class="btn btn-primary btn-block">Filtrar</button>
      </div>
  </form>
</div>
<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th scope="col" class="col-md-1">Fecha</th>
      <th scope="col" class="col-md-1">Nombre</th>
      <th scope="col" class="col-md-1">Email</th>
      <th scope="col" class="col-md-1">Telefono</th>
      <th scope="col" class="col-md-1">Horario de entrega</th>*
      <th scope="col" class="col-md-1">Costo entrega</th>
      <th scope="col" class="col-md-1">Nombre entrega</th>
      <th scope="col" class="col-md-1">De</th>
      <th scope="col" class="col-md-1">Para</th>
      <th scope="col" class="col-md-1">Mensaje</th>
      <th scope="col" class="col-md-1">Sector entrega</th>
      <th scope="col" class="col-md-1">Direccion entrega</th>
      <th scope="col" class="col-md-1">Instrucciones entrega</th>
      <th scope="col" class="col-md-1">Fecha entrega</th>
      <th scope="col" class="col-md-1">Forma de Pago</th>
      <th scope="col" class="col-md-1">Precio</th>
      <th scope="col" class="col-md-1">Estado</th>
      <th scope="col" class="col-md-1">Items</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pedidos.each do |pedido| %>
      <tr>
        <td><%= pedido.fecha %></td>
        <td><%= pedido.nombre %></td>
        <td><%= pedido.email %></td>
        <td><%= pedido.telefono %></td>
        <td><%= pedido.horario_entrega %></td>
        <td><%= pedido.costo_entrega %></td>
        <td><%= pedido.nombre_entrega %></td>
        <td><%= pedido.de %></td>
        <td><%= pedido.para %></td>
        <td><%= pedido.mensaje %></td>
        <td><%= pedido.sector_entrega %></td>
        <td><%= pedido.direccion_entrega %></td>
        <td><%= pedido.instrucciones_entrega %></td>
        <td><%= pedido.fecha_entrega.to_s %></td>
        <td><%= pedido.forma_pago_str %></td>
        <td><%= pedido.total %></td>
        <td><span class="<%= pedido.estado.downcase %>_label"><%= pedido.estado %></span></td>
        <td>
          <% pedido.items_json.each do |item| %>
          <div>
            <img src="<%= item.producto.foto_url %>" width="100" /> <%= item.cantidad %> - <%= item.producto.nombre %>
          </div>
          <% end %>
        </td>
        <td><%= link_to 'Show', pedido, :class => 'btn btn-info' %></td>
        <td><%= link_to 'Edit', edit_pedido_path(pedido), :class => 'btn btn-info' %></td>
        <td><%= link_to 'Destroy', pedido, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-danger' %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br />
<!--<%= link_to 'New Pedido', new_pedido_path, :class => 'btn btn-primary' %>-->
